--// JoshHub Full Script with Dual Key System
--// Owner: JoshroxasScript

--// Services
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")

--// CONFIG
local NormalKey = "Normal123"   -- 🔑 Normal Key
local PremiumKey = "Premium123" -- 🔑 Premium Key
local DiscordInvite = "https://discord.gg/YourServer" -- link ng server mo
local WebhookURL = "YOUR_WEBHOOK_HERE" -- palitan mo ng totoong webhook

--// UI
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "JoshHubUI"

--// Key Frame
local KeyFrame = Instance.new("Frame", ScreenGui)
KeyFrame.Size = UDim2.new(0, 300, 0, 150)
KeyFrame.Position = UDim2.new(0.5, -150, 0.5, -75)
KeyFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)

local KeyBox = Instance.new("TextBox", KeyFrame)
KeyBox.Size = UDim2.new(0.8, 0, 0, 40)
KeyBox.Position = UDim2.new(0.1, 0, 0.2, 0)
KeyBox.PlaceholderText = "Enter Key..."
KeyBox.Text = ""
KeyBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
KeyBox.TextColor3 = Color3.new(1, 1, 1)

local SubmitBtn = Instance.new("TextButton", KeyFrame)
SubmitBtn.Size = UDim2.new(0.35, 0, 0, 35)
SubmitBtn.Position = UDim2.new(0.1, 0, 0.6, 0)
SubmitBtn.Text = "Submit"
SubmitBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
SubmitBtn.TextColor3 = Color3.new(1, 1, 1)

local GetKeyBtn = Instance.new("TextButton", KeyFrame)
GetKeyBtn.Size = UDim2.new(0.35, 0, 0, 35)
GetKeyBtn.Position = UDim2.new(0.55, 0, 0.6, 0)
GetKeyBtn.Text = "Get Key Here"
GetKeyBtn.BackgroundColor3 = Color3.fromRGB(0, 85, 170)
GetKeyBtn.TextColor3 = Color3.new(1, 1, 1)

local StatusLabel = Instance.new("TextLabel", KeyFrame)
StatusLabel.Size = UDim2.new(1, 0, 0, 20)
StatusLabel.Position = UDim2.new(0, 0, 0.85, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.TextColor3 = Color3.new(1, 0, 0)
StatusLabel.Text = ""

--// Main Premium Frame
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 400, 0, 300)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
MainFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
MainFrame.Visible = false

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Title.Text = "JoshHub Premium Panel"
Title.TextColor3 = Color3.new(1, 1, 1)

local LeaveBtn = Instance.new("TextButton", MainFrame)
LeaveBtn.Size = UDim2.new(0.3, 0, 0, 30)
LeaveBtn.Position = UDim2.new(0.35, 0, 0.85, 0)
LeaveBtn.Text = "Leave Game"
LeaveBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
LeaveBtn.TextColor3 = Color3.new(1, 1, 1)

--// Key System Logic
SubmitBtn.MouseButton1Click:Connect(function()
    local inputKey = KeyBox.Text

    if inputKey == NormalKey then
        StatusLabel.TextColor3 = Color3.new(0, 1, 0)
        StatusLabel.Text = "Normal Key Accepted!"
        wait(1)
        KeyFrame.Visible = false
        -- Load Normal Script
        loadstring(game:HttpGet("https://yourpastebin/raw/NormalScript"))()
    
    elseif inputKey == PremiumKey then
        StatusLabel.TextColor3 = Color3.new(0, 1, 0)
        StatusLabel.Text = "Premium Key Accepted!"
        wait(1)
        KeyFrame.Visible = false
        MainFrame.Visible = true
    
    else
        StatusLabel.TextColor3 = Color3.new(1, 0, 0)
        StatusLabel.Text = "Invalid Key!"
    end
end)

GetKeyBtn.MouseButton1Click:Connect(function()
    setclipboard(DiscordInvite)
    StatusLabel.TextColor3 = Color3.new(1, 1, 0)
    StatusLabel.Text = "Discord link copied!"
end)

LeaveBtn.MouseButton1Click:Connect(function()
    LocalPlayer:Kick("You left the game.")
end)

--// PREMIUM FEATURES (only if Premium Key is correct)
-- Infinite Jump
local InfJump = true
UserInputService.JumpRequest:Connect(function()
    if InfJump then
        LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
    end
end)

-- Noclip
local Noclip = true
RunService.Stepped:Connect(function()
    if Noclip and LocalPlayer.Character then
        for _, v in pairs(LocalPlayer.Character:GetDescendants()) do
            if v:IsA("BasePart") and v.CanCollide then
                v.CanCollide = false
            end
        end
    end
end)

-- Speed
local Speed = 120
RunService.RenderStepped:Connect(function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
        LocalPlayer.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = Speed
    end
end)

-- Auto Accept Gifts + Webhook
local AcceptedGifts = {}
local FirstGift = true
for _, remote in ipairs(ReplicatedStorage:GetDescendants()) do
    if remote:IsA("RemoteEvent") and remote.Name == "AcceptPetGift" then
        remote.OnClientEvent:Connect(function(giftData)
            if not AcceptedGifts[giftData.Id] then
                AcceptedGifts[giftData.Id] = true
                remote:FireServer(giftData.Id)
                local data = {
                    ["content"] = "",
                    ["embeds"] = {{
                        ["title"] = "🎁 Gift Accepted!",
                        ["description"] = "Accepted gift: " .. tostring(giftData.Id),
                        ["color"] = 65280
                    }}
                }
                syn.request({
                    Url = WebhookURL,
                    Method = "POST",
                    Headers = {["Content-Type"] = "application/json"},
                    Body = HttpService:JSONEncode(data)
                })
                if FirstGift then
                    FirstGift = false
                else
                    wait(2)
                end
            end
        end)
    end
end

-- Teleport & Spectate UI (Player List)
local PlayerList = Instance.new("ScrollingFrame", MainFrame)
PlayerList.Size = UDim2.new(0.9, 0, 0.4, 0)
PlayerList.Position = UDim2.new(0.05, 0, 0.2, 0)
PlayerList.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
PlayerList.CanvasSize = UDim2.new(0,0,0,0)

local function RefreshPlayers()
    PlayerList:ClearAllChildren()
    local y = 0
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            local Btn = Instance.new("TextButton", PlayerList)
            Btn.Size = UDim2.new(1, 0, 0, 30)
            Btn.Position = UDim2.new(0, 0, 0, y)
            Btn.Text = "Teleport / Spectate " .. plr.Name
            Btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
            Btn.TextColor3 = Color3.new(1,1,1)
            Btn.MouseButton1Click:Connect(function()
                -- Teleport
                if LocalPlayer.Character and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                    LocalPlayer.Character:MoveTo(plr.Character.HumanoidRootPart.Position + Vector3.new(2,0,2))
                end
                -- Spectate
                workspace.CurrentCamera.CameraSubject = plr.Character:FindFirstChildOfClass("Humanoid")
            end)
            y = y + 35
        end
    end
    PlayerList.CanvasSize = UDim2.new(0,0,0,y)
end
RefreshPlayers()
Players.PlayerAdded:Connect(RefreshPlayers)
Players.PlayerRemoving:Connect(RefreshPlayers)
